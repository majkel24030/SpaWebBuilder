services:
  # Backend API Service
  - type: web
    name: windoor-config-backend
    env: python
    plan: free
    region: frankfurt
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn backend.app.main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.8"
      - key: ENVIRONMENT
        value: "production"
      - key: BACKEND_CORS_ORIGINS
        value: '["*"]'
      - key: SECRET_KEY
        generateValue: true
      - key: ALGORITHM
        value: "HS256"
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: "30"

  # Frontend Static Site
  - type: static
    name: windoor-config-frontend
    buildCommand: "cd frontend && npm ci && npm run build && cp public/_redirects dist/"
    publishPath: "frontend/dist"
    envVars:
      - key: NODE_VERSION
        value: "18"
      - key: VITE_API_URL
        value: "https://windoor-config-backend.onrender.com/api"
      - key: VITE_ENVIRONMENT
        value: "production"

# Database
databases:
  - name: windoor-config-db
    databaseName: windoor_config
    user: windoor_user
    plan: free
    region: frankfurt